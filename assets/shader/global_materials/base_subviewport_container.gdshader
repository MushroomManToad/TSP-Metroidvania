shader_type canvas_item;

uniform vec2 subpixel_offset = vec2(0.0, 0.0);

void fragment() {
    //vec2 uv_offset = subpixel_offset / vec2(640.0, 360.0);
    //COLOR = texture(TEXTURE, UV + uv_offset);
}